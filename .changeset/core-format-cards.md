---
"@aldus-palace/core": minor
---

Add `formatActionCard` and `formatTodayText`: locale-aware text projections of
the ActionCard and the Today payload. Pure functions, shared by the examples and
the MCP tool returns.

# CLAUDE.md

@AGENTS.md

## Verify in one command

```bash
pnpm install
pnpm verify     # typecheck · test · eval · build · spec:check
pnpm demo       # the offline capture demo, no API key
```

Before judging a claim, read [`EVALUATION.md`](EVALUATION.md): every public claim
maps to the command or file that proves it, and the parts that are not built yet
are listed there.
